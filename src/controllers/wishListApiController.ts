import axios from 'axios';

export const wishListApiController = async () => {
  try {
    const respone = await axios('');
  } catch (error) {
    console.log('wishList Api call error', error);
  }
};
